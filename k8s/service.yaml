apiVersion: v1
kind: Service
metadata:
  name: deployguard-analysis
  namespace: deployguard
  labels:
    app: deployguard-analysis
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: deployguard-analysis
